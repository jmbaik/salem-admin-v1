const thumbsContainer = {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginTop: 16,
};
const thumb = {
    display: 'inline-flex',
    borderRadius: 
}

const ComingWayGuide = () => {
    return <div>ComingWayGuide</div>;
};

export default ComingWayGuide;
